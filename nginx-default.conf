server{

  listen 80;
  server_name localhost;

  root /tilelive/tiles/;

  location ~* ^/tiles/(?<tile_path>.*)$ {
    try_files $tile_path @node;
  }

  location @node {
    proxy_pass http://localhost:8888;
  }

}
